import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<ejs-switch [checked]='true'></ejs-switch>` 
})
export class AppComponent {
  title = 'angular11Switch-app';
}
